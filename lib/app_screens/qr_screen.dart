import 'package:flutter/material.dart';

class QRSreen extends StatelessWidget {
  const QRSreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
